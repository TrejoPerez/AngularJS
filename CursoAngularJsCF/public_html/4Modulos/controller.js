angular.module("MyFirstApp",[])
        /*Anguar module no solol es nua forma para crear modulos, es una forma para registrar los modulos
         * del framework
         * Cuando solo se llama a la dependecia con un solo modulo, como se hace en el index
         * Cuando solo se llama la funcon con un paramtetro, lo qu se hace es buscar en el registro
         * del framework y traer la referencia al modulo creado con el mismo nombre
         * 
         * 
         * Con dos paramteotrs uno es el nombre del modulo y el otro las depedencias
         * 
         * 
         * Aqui se registra y si se usa en html se manda a llamar el modulo
        .controller("FirstController",function($scope){
   $scope.nombre="CF" ;
}).controller("SecondController",function($scope){
    
});
*/